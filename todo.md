# Remember Us 技术 TODO

> 对照《方案速览 v3》的技术方案整理。当前仓库已经具备可交互的前端 Demo，但核心业务仍运行在单页 HTML、`localStorage` 和 `BroadcastChannel` 中。

## 进度总览

- 待完成总数：**38 项**
- P0｜真实技术闭环：**14 项**
- P1｜可用技术 MVP：**14 项**
- P2｜上线准备：**10 项**
- 当前完成度：**0 / 38**（仅统计以下技术任务，不把现有高保真 Demo 重复计入）

## 已有基础（不计入待完成数）

- [x] 家人端与老人端高保真可交互界面
- [x] 创建家庭、输入家庭码和人物资料的前端流程
- [x] 基于 `localStorage` 的本机数据保存
- [x] 基于 `BroadcastChannel` 的同浏览器多窗口同步
- [x] 模拟来电、固定问答、虚拟定位和回家引导流程

---

## P0｜真实技术闭环（14 项）

目标：把当前本机模拟 Demo 升级为两部真实设备可以共同使用、至少一条 AI 链路可以跑通的联网 Demo。

### 后端与数据

- [ ] **P0-01 建立业务后端与环境配置**：提供独立的 API 入口，密钥只保存在服务端，前端代码和浏览器请求中不暴露 Gemini、数据库或地图密钥。
- [ ] **P0-02 建立结构化数据库**：至少包含家庭、家庭成员、老人档案、人物档案、事实条目、待办、位置事件和风险事件，并能完成创建、查询与更新。
- [ ] **P0-03 服务端生成并校验家庭码**：家庭码由后端生成且保证唯一；另一部设备输入后能加入同一家庭，错误码、过期码和不存在的码都有明确反馈。
- [ ] **P0-04 实现跨设备实时同步**：用 WebSocket、Firebase 或等价共享状态服务替代 `BroadcastChannel`；两部不同手机修改资料或触发事件时能在 2 秒内同步。

### Gemini 与家庭记忆

- [ ] **P0-05 接入通话音频输入**：能够上传或录制一段测试通话音频，并由服务端安全转交 Gemini；失败时可重试且不会丢失当前页面状态。
- [ ] **P0-06 实现通话转写与结构化抽取**：Gemini 按 JSON Schema 返回事实、待办、来源和“是否需要家人确认”，无效输出会被服务端拒绝而不是直接写库。
- [ ] **P0-07 实现家人确认闭环**：通话抽取结果进入待确认列表；确认、修改或忽略后，数据库中的状态和内容正确更新。
- [ ] **P0-08 实现家庭事实检索**：问答只检索当前家庭中“已确认且未过期”的事实，不能读到其他家庭或未确认数据。
- [ ] **P0-09 接入受控问答生成**：Gemini 只能根据检索结果组织短句；没有可靠事实时固定拒答并创建家人端待补充问题。

### 语音、定位与引导

- [ ] **P0-10 接入老人端语音输入**：使用浏览器 Web Speech API 或等价方案识别老人问题，同时保留可点击问题作为降级入口。
- [ ] **P0-11 接入 TTS 语音播报**：问答与回家指令可以真实播放，支持慢速、短句、暂停和重复播放，并遵循系统静音及权限限制。
- [ ] **P0-12 接入真实 GPS 定位**：经用户授权获取位置并周期上报；拒绝权限、信号中断和低精度定位都有可理解的降级状态。
- [ ] **P0-13 实现基础风险规则**：服务端根据真实位置判断地理围栏越界和持续远离，规则结果包含触发时间、位置、距离和可解释原因。
- [ ] **P0-14 接入真实路线与地标指引**：从地图路线和地点服务获得步行路线，生成一次一步的指令；找不到可靠地标时降级为距离与转向。

---

## P1｜可用技术 MVP（14 项）

目标：补齐安全、可靠性和方案中承诺的完整规则，使系统可供小范围真实家庭测试。

### 账户、权限与隐私

- [ ] **P1-01 实现家人身份认证**：支持可靠的登录、退出、会话过期和找回流程，未登录用户不能访问家庭数据。
- [ ] **P1-02 实现家庭角色权限**：区分创建者、普通家人与老人设备；敏感资料修改、成员管理和家庭删除仅允许有权限的角色执行。
- [ ] **P1-03 建立老人端设备配对凭证**：家庭码只用于首次配对，成功后换成可撤销的设备凭证，避免长期用六位数字访问家庭数据。
- [ ] **P1-04 加密敏感数据与传输**：所有接口使用 HTTPS；数据库、对象存储和备份中的个人资料、位置及事实按敏感数据策略保护。
- [ ] **P1-05 实现录音与位置授权管理**：首次使用前清楚说明用途、保存范围和关闭方式；撤回授权后立即停止采集。
- [ ] **P1-06 落实数据最小化与删除策略**：不长期保存通话原始音频；用户可删除人物、事实、位置记录或整个家庭，相关副本按策略清理。

### 记忆与安全规则

- [ ] **P1-07 实现事实来源和有效期**：每条事实记录来源、确认人、确认时间和过期时间；过期事实自动退出问答检索。
- [ ] **P1-08 实现事实冲突处理**：新旧事实矛盾时保留两个版本并交给家人裁决，不自动覆盖已确认事实。
- [ ] **P1-09 实现完整风险规则**：在越界和持续远离之外，增加原地徘徊、超时未归、定位长时间中断及恢复规则，并避免重复告警。
- [ ] **P1-10 实现风险通知与解除闭环**：风险触发后老人端先开始引导、家人端收到实时提醒；回家或家人确认安全后，两端状态一致并记录处理结果。
- [ ] **P1-11 支持家人预设可靠地标**：家人可以维护老人认识的地点、照片或描述，路线生成优先使用这些地标。

### 稳定性与测试

- [ ] **P1-12 增加接口校验、限流和幂等性**：重复提交不会产生重复事实或告警；异常参数、越权请求和高频调用会被拒绝并记录。
- [ ] **P1-13 增加离线与弱网恢复**：断网时保留未提交操作，恢复网络后按顺序同步；冲突不会静默覆盖用户数据。
- [ ] **P1-14 建立自动化测试体系**：覆盖家庭隔离、事实确认与拒答、风险规则、跨端同步和关键端到端流程，并在持续集成中自动运行。

---

## P2｜上线准备（10 项）

目标：达到可部署、可监控、可维护的发布状态。

- [ ] **P2-01 拆分单文件应用**：把 `public/app/index.html` 中的样式、状态、数据访问和业务场景拆成职责清晰的模块，同时保持现有交互不回退。
- [ ] **P2-02 清理原型依赖与元数据**：移除剩余 Lovable 标题、作者、错误上报和不再使用的依赖，补齐正式产品的 SEO、图标和隐私说明。
- [ ] **P2-03 完成无障碍验证**：老人端和家人端满足键盘操作、屏幕阅读器标签、对比度、焦点状态、字号放大和至少 44×44 px 触控目标要求。
- [ ] **P2-04 完成多设备兼容测试**：覆盖主流 Android、iPhone、微信内置浏览器及常见屏幕尺寸，并记录语音、定位权限的能力差异。
- [ ] **P2-05 建立错误监控与业务可观测性**：采集接口错误、AI 失败率、同步延迟、定位中断和风险事件处理时长，但日志不得包含完整敏感内容。
- [ ] **P2-06 建立成本与配额保护**：为 Gemini、地图、TTS 和实时同步设置预算、配额、超限降级与告警，避免异常调用造成不可控费用。
- [ ] **P2-07 建立 CI/CD 发布流程**：代码合并后自动执行格式检查、测试和构建；通过审核后部署到测试或生产环境，并支持快速回滚。
- [ ] **P2-08 完成生产部署**：配置正式域名、HTTPS、环境变量、数据库迁移和备份恢复，并通过一次完整的部署验收。
- [ ] **P2-09 完成隐私与安全评审**：检查鉴权、数据隔离、密钥管理、依赖漏洞、隐私告知和数据删除流程，关闭所有阻断发布的问题。
- [ ] **P2-10 编写运维与演示文档**：README 包含架构、环境配置、启动、测试、部署和故障恢复说明；准备不依赖生产隐私数据的演示家庭与重置流程。

---

## 建议执行顺序

1. 先完成 P0-01 至 P0-04，建立后端、数据库和真实双端连接。
2. 再完成 P0-05 至 P0-09，打通“通话 → 抽取 → 确认 → 问答”主链路。
3. 然后完成 P0-10 至 P0-14，打通“语音 → 定位 → 风险 → 回家引导”主链路。
4. P1 按“身份权限 → 隐私 → 记忆规则 → 风险闭环 → 稳定性测试”的顺序推进。
5. P2 在准备公开部署前完成，其中 P2-03、P2-05、P2-06 和 P2-09 不应留到上线之后。

## 计数规则

- 只有上方 `- [ ]` 的 P0、P1、P2 编号任务计入待完成总数。
- “已有基础”的 `- [x]` 只用于说明现状，不计入 38 项。
- 一个任务只有在其完整完成标准满足后才能勾选；部分接入或仅有静态界面仍记为未完成。
